import React from "react";

// develop the logic to switch between themes from this array themes = ['base', 'rainforrest', 'candy']
const themes = ["base", "rainforrest", "candy"];

const handleTheme = () => {};

const ButtonThemeSwitcher = () => {
  return <button onClick={() => console.log("Hello Baby!!")}>Theme</button>;
};

export default ButtonThemeSwitcher;
